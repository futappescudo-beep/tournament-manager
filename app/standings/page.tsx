import { Trophy } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { getStandings, goalDifference, points } from "@/lib/service/competition.service";

export default async function StandingsPage() {
  const standings = await getStandings();
  return <AppShell><div className="space-y-6"><div><p className="text-xs font-bold tracking-[.18em] text-[var(--ea-gold)]">COMPETENCIA</p><h1 className="mt-1 text-3xl text-white">Tabla de posiciones</h1><p className="mt-1 text-sm text-stone-400">3 puntos por victoria, 1 por empate.</p></div><section className="ea-panel overflow-hidden rounded-lg"><div className="flex items-center gap-3 border-b border-[var(--ea-border)] p-5"><Trophy className="text-[var(--ea-gold)]" /><h2 className="ea-heading text-xl">Clasificacion general</h2></div>{standings.length ? <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-sm"><thead className="border-b border-[var(--ea-border)] text-left text-xs uppercase tracking-wider text-stone-500"><tr><th className="p-4">#</th><th>Equipo</th><th>PJ</th><th>PG</th><th>PE</th><th>PP</th><th>GF</th><th>GC</th><th>DG</th><th className="text-[var(--ea-gold)]">Pts</th></tr></thead><tbody>{standings.map((standing, index) => <tr key={standing.team_registration_id ?? index} className="border-b border-[var(--ea-border)]/70"><td className="p-4"><span className="inline-grid h-6 w-6 place-items-center rounded bg-[#2f6e29] font-bold text-white">{index + 1}</span></td><td className="font-semibold">{standing.display_name ?? "Equipo"}</td><td>{standing.played ?? 0}</td><td>{standing.won ?? 0}</td><td>{standing.drawn ?? 0}</td><td>{standing.lost ?? 0}</td><td>{standing.goals_for ?? 0}</td><td>{standing.goals_against ?? 0}</td><td>{goalDifference(standing)}</td><td className="font-bold text-[var(--ea-gold-soft)]">{points(standing)}</td></tr>)}</tbody></table></div> : <div className="grid min-h-60 place-items-center p-8 text-center"><p className="text-sm text-stone-400">La tabla se calculara cuando haya resultados cargados.</p></div>}</section></div></AppShell>;
}
