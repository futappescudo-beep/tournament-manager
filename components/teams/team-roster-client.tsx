"use client";

import { useState } from "react";
import { UserPlus, UserRound } from "lucide-react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { assignPlayerToTeam } from "@/lib/actions/players";
import type { Player } from "@/lib/types/player";

type RosterEntry = { id: string; shirt_number: number; is_captain: boolean; is_goalkeeper: boolean; team_registration_id: string; players: { first_name: string; last_name: string; document_number: string; photo_url: string | null } | null };
type Registration = { id: string; label: string };

export function TeamRosterClient({ players, registrations, availablePlayers }: { players: RosterEntry[]; registrations: Registration[]; availablePlayers: Player[] }) {
  const [open, setOpen] = useState(false);
  const [loading, setLoading] = useState(false);
  const router = useRouter();
  const labels = new Map(registrations.map((registration) => [registration.id, registration.label]));
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); setLoading(true); try { await assignPlayerToTeam({ player_id: String(form.get("playerId")), team_registration_id: String(form.get("registrationId")), shirt_number: Number(form.get("shirtNumber")), is_captain: form.get("captain") === "on", is_goalkeeper: form.get("goalkeeper") === "on" }); toast.success("Jugador asignado al plantel."); setOpen(false); router.refresh(); } catch (error) { toast.error(error instanceof Error ? error.message : "No se pudo asignar el jugador."); } finally { setLoading(false); } }
  return <><section className="ea-panel overflow-hidden rounded-lg">{players.length ? <div className="divide-y divide-[var(--ea-border)]">{players.map((entry) => <article key={entry.id} className="flex items-center gap-4 p-4"><span className="grid h-10 w-10 place-items-center rounded-md bg-[#29180d] font-bold text-[var(--ea-gold)]">#{entry.shirt_number}</span>{entry.players?.photo_url ? <img src={entry.players.photo_url} alt="" className="h-10 w-10 rounded-full object-cover" /> : <UserRound className="h-8 w-8 text-stone-500" />}<div><p className="font-semibold">{entry.players?.first_name} {entry.players?.last_name}</p><p className="text-xs text-stone-500">DNI: {entry.players?.document_number} · {labels.get(entry.team_registration_id) ?? "Equipo"}</p></div><div className="ml-auto flex gap-2 text-xs">{entry.is_captain && <span className="rounded bg-[#9e171b] px-2 py-1">Capitán</span>}{entry.is_goalkeeper && <span className="rounded bg-[#a66b0e] px-2 py-1">Arquero</span>}</div></article>)}</div> : <div className="grid min-h-60 place-items-center p-8 text-center"><div><p className="text-sm text-stone-400">Todavía no hay jugadores asignados a este equipo.</p><button onClick={() => setOpen(true)} className="mt-4 inline-flex items-center gap-2 rounded-md bg-[linear-gradient(135deg,#b71920,#77080d)] px-4 py-2 text-sm font-bold text-white"><UserPlus size={16} />Asignar jugador existente</button></div></div>}</section>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-lg rounded-xl border border-[var(--ea-border)] bg-[#121313] p-6 shadow-2xl"><h2 className="text-xl font-bold text-white">Asignar jugador al plantel</h2><p className="mt-2 text-sm text-stone-400">Elegí un jugador existente y la zona donde competirá.</p><div className="mt-5 space-y-4"><label className="grid gap-2 text-sm font-medium">Jugador<select required name="playerId" defaultValue=""><option value="" disabled>Seleccionar jugador</option>{availablePlayers.map((player) => <option key={player.id} value={player.id}>{player.last_name}, {player.first_name} · {player.document_number}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Equipo y zona<select required name="registrationId" defaultValue={registrations[0]?.id ?? ""}>{registrations.map((registration) => <option key={registration.id} value={registration.id}>{registration.label}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Número de camiseta<input required name="shirtNumber" type="number" min="1" max="99" defaultValue="1" /></label><div className="flex gap-5 text-sm"><label className="flex items-center gap-2"><input name="captain" type="checkbox" />Capitán</label><label className="flex items-center gap-2"><input name="goalkeeper" type="checkbox" />Arquero</label></div></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setOpen(false)} className="rounded-md border border-[var(--ea-border)] px-4 py-2 text-sm">Cancelar</button><button disabled={loading || !availablePlayers.length} className="rounded-md bg-[linear-gradient(135deg,#b71920,#77080d)] px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{loading ? "Asignando..." : "Asignar jugador"}</button></div></form></div>}</>;
}
